

// in here Type mean that accept like any any values but if number accpet it will return number if string accept it will return string

// accoring to input return is gneric type


function one<Type>(val:Type):Type{
    return val
}

export {}